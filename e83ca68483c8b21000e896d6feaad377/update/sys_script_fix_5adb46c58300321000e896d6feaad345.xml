<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_script_fix">
    <sys_script_fix action="INSERT_OR_UPDATE">
        <before>false</before>
        <description/>
        <name>Cisco Secure Access Event Changes</name>
        <record_for_rollback>true</record_for_rollback>
        <script><![CDATA[(function() {
    // 1. Create or update custom fields on em_event
    function createOrUpdateField(tableName, columnName, label, type, length) {
        var dict = new GlideRecord('sys_dictionary');
        dict.addQuery('name', tableName);
        dict.addQuery('element', columnName);
        dict.query();

        if (dict.next()) {
            var updated = false;
            if (dict.max_length != length) {
                dict.max_length = length;
                updated = true;
            }
            if (updated) {
                dict.update();
                gs.print("]]>🔄<![CDATA[ Field " + columnName + " updated in " + tableName);
            } else {
                gs.print("⚠️ Field " + columnName + " already exists in " + tableName);
            }
        } else {
            var newDict = new GlideRecord('sys_dictionary');
            newDict.initialize();
            newDict.name = tableName;
            newDict.element = columnName;
            newDict.column_label = label;
            newDict.internal_type = type;
            newDict.max_length = length;
            newDict.mandatory = false;
            newDict.active = true;
            newDict.insert();
            gs.print("✅ Field " + columnName + " created in " + tableName);
        }
    }

    // Example fields
    createOrUpdateField("em_event", "u_api_key", "API Key", "string", 100);
    createOrUpdateField("em_event", "u_rule_name", "Rule Name", "string", 2000);

    // 2. Create or update Event Rule
    function createOrUpdateEventRule(name, sysId, condition) {
        var rule = new GlideRecord('em_event_rule');
        if (rule.get(sysId)) {
            gs.print("]]>🔄<![CDATA[ Updating Event Rule: " + name);
            rule.name = name;
            rule.active = true;
            rule.ignore = true; // ✅ check "Ignore matching events"
            rule.filter_condition = condition;
            rule.update();
            gs.print("✅ Event Rule " + name + " updated.");
        } else {
            rule.initialize();
            rule.sys_id = sysId; // only if you want fixed sys_id
            rule.name = name;
            rule.active = true;
            rule.ignore = true; // ✅ important
            rule.filter_condition = condition;
            rule.insert();
            gs.print("✅ Event Rule " + name + " created.");
        }
    }

    var condition = "severity=2^ORseverity=3^ORseverity=0^ORseverity=4";
    createOrUpdateEventRule(
        "Cisco Secure Access Event Management Rule",
        "abcdef1234567890abcdef1234567890", // fixed sys_id
        condition
    );

    gs.print("]]>🎯<![CDATA[ Global setup script complete.");
})();
]]></script>
        <sys_class_name>sys_script_fix</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-09-16 05:57:40</sys_created_on>
        <sys_id>5adb46c58300321000e896d6feaad345</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_name>Cisco Secure Access Event Changes</sys_name>
        <sys_package display_value="Cisco Secure Access Connector for ServiceNow Gruve" source="e83ca68483c8b21000e896d6feaad377">e83ca68483c8b21000e896d6feaad377</sys_package>
        <sys_policy/>
        <sys_scope display_value="Cisco Secure Access Connector for ServiceNow Gruve">e83ca68483c8b21000e896d6feaad377</sys_scope>
        <sys_update_name>sys_script_fix_5adb46c58300321000e896d6feaad345</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-09-16 05:57:40</sys_updated_on>
        <unloadable>false</unloadable>
    </sys_script_fix>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>5adb46c58300321000e896d6feaad345</id>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-09-16 05:57:39</sys_created_on>
        <sys_id>3b7e020d8300321000e896d6feaad319</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-09-16 05:57:39</sys_updated_on>
        <table>sys_script_fix</table>
        <use_es_latest>true</use_es_latest>
    </sys_es_latest_script>
</record_update>
